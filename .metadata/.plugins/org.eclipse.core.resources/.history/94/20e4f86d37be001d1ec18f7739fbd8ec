import java.util.Arrays;
import java.util.stream.Collectors;
import java.util.stream.Stream;

public class Datedate {

	public static void main(String[] args) {
		// capitalize 1st and last
		String s = "shivani aghav";
//        Stream.of(s.split(" ")).map(t->t.substring(0,1).toUpperCase()+t.substring(1)+" ").forEach(System.out::print);
//		Stream.of(s.split(" ")).map(t -> t.substring(0, 1).toUpperCase() + t.substring(1, t.length() - 1)
//				+ t.substring(t.length() - 1).toUpperCase() + " ").forEach(System.out::print);

//		Stream.of(s.split(" ")).map(t -> t.substring(0,1).toUpperCase()+t.substring(1)+" ").forEach(System.out::print);
//		System.out.println("java".compareTo("java"));
		
		//[ece, ele, ere]
		String[] s2 = { "ele", "ece", "ere" };
//		Arrays.sort(s2);
//		System.out.println(Arrays.toString(s2));
		
		java.time.LocalDate localDate = java.time.LocalDate.of(2023, 03, 8);
        java.time.DayOfWeek dayOfWeek = localDate.getDayOfWeek();
        System.out.println(dayOfWeek.toString());
	}
}
